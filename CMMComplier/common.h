#pragma once
#include<string>
#include<sstream>
#include<vector>
using namespace std;
std::string itos(int n);

std::string dtos(double d);

std::string ctos(char ch);

static std::vector<int> globalVarVec;

